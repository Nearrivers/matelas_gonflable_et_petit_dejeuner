<%= form_with(model: user_fav) do |form| %>
  <% if user_fav.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_fav.errors.count, "error") %> prohibited this user_fav from being saved:</h2>

      <ul>
        <% user_fav.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.text_field :location_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
