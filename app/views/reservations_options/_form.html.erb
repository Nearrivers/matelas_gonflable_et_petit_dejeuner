<%= form_with(model: reservations_option) do |form| %>
  <% if reservations_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reservations_option.errors.count, "error") %> prohibited this reservations_option from being saved:</h2>

      <ul>
        <% reservations_option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reservation_id %>
    <%= form.text_field :reservation_id %>
  </div>

  <div class="field">
    <%= form.label :option_id %>
    <%= form.text_field :option_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
