<%= form_with(model: location_appliance) do |form| %>
  <% if location_appliance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location_appliance.errors.count, "error") %> prohibited this location_appliance from being saved:</h2>

      <ul>
        <% location_appliance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.text_field :location_id %>
  </div>

  <div class="field">
    <%= form.label :appliance_id %>
    <%= form.text_field :appliance_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
